output_path_template = "{folder}/{artist} - {title}.mp3"

SPOTDL_SETTINGS = {
    "--bitrate": "160k",
    "--format": "mp3",
    "--skip-album-art": None,
    "--print-errors": None,
    "--overwrite": "skip",
    "--threads": "8",
    "--output": None
}